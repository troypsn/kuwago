<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#0D1117"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center">

    <!-- Shield blocked icon -->
    <ImageView
        android:layout_width="88dp"
        android:layout_height="88dp"
        android:src="@drawable/ic_block"
        app:tint="@color/color_alert"
        android:contentDescription="Connection blocked" />

    <!-- Title -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:fontFamily="sans-serif-black"
        android:text="🛡️ CONNECTION BLOCKED"
        android:textColor="@color/color_alert"
        android:textSize="22sp"
        android:gravity="center" />

    <!-- Subtitle -->
    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:text="Kuwago URL Shield prevented access to a known phishing site."
        android:textColor="#B0B0B0"
        android:textSize="13sp" />

    <!-- Blocked site card -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="28dp"
        android:background="@drawable/bg_action_card"
        android:orientation="vertical"
        android:padding="18dp">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="BLOCKED SITE"
            android:textColor="#FF5370"
            android:textSize="10sp"
            android:fontFamily="sans-serif-medium"
            android:letterSpacing="0.12" />

        <TextView
            android:id="@+id/vpn_blocked_hostname"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="6dp"
            android:fontFamily="sans-serif-bold"
            android:text="phishing-site.com"
            android:textColor="@color/text_primary"
            android:textSize="18sp" />

        <View
            android:layout_width="match_parent"
            android:layout_height="1dp"
            android:layout_marginTop="14dp"
            android:layout_marginBottom="14dp"
            android:background="#2A2A2A" />

        <TextView
            android:id="@+id/vpn_blocked_detail"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:text="This domain was previously identified as a phishing destination by Kuwago's scanner. The connection was blocked to protect your device and personal information."
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            android:lineSpacingMultiplier="1.4" />
    </LinearLayout>

    <!-- What this means card -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:background="@drawable/bg_action_card"
        android:orientation="horizontal"
        android:padding="14dp"
        android:gravity="center_vertical">

        <ImageView
            android:layout_width="24dp"
            android:layout_height="24dp"
            android:src="@drawable/ic_shield"
            app:tint="#4CAF50"
            android:contentDescription="protected" />

        <TextView
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_weight="1"
            android:layout_marginStart="12dp"
            android:text="Your device is protected. No data was sent to this site."
            android:textColor="#80E0A0"
            android:textSize="13sp"
            android:fontFamily="sans-serif-medium" />

    </LinearLayout>

    <!-- Action buttons -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="32dp"
        android:orientation="vertical">

        <!-- Primary: Dismiss -->
        <androidx.appcompat.widget.AppCompatButton
            android:id="@+id/btn_vpn_blocked_dismiss"
            android:layout_width="match_parent"
            android:layout_height="52dp"
            android:background="@drawable/bg_status_badge"
            android:backgroundTint="@color/color_alert"
            android:fontFamily="sans-serif-bold"
            android:text="✓ I'm Protected — Dismiss"
            android:textColor="@color/white"
            android:textSize="14sp" />

        <!-- Secondary: View scan history -->
        <TextView
            android:id="@+id/btn_vpn_blocked_view_history"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="18dp"
            android:padding="8dp"
            android:text="View scan history"
            android:textColor="#7090B8"
            android:textSize="13sp"
            android:fontFamily="sans-serif-medium"
            android:clickable="true"
            android:focusable="true" />

        <!-- Destructive: Disable VPN -->
        <TextView
            android:id="@+id/btn_vpn_blocked_disable_shield"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginTop="8dp"
            android:padding="8dp"
            android:text="Disable URL Shield (not recommended)"
            android:textColor="#585858"
            android:textSize="12sp"
            android:fontFamily="sans-serif"
            android:clickable="true"
            android:focusable="true" />

    </LinearLayout>

</LinearLayout>
